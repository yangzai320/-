# Data

The raw UCI Online Retail dataset is not included in the lightweight GitHub version to keep the repository small.

Run this command to download and process the data automatically:

```bash
python src/run_analysis.py
```

The script downloads the official UCI zip file and extracts `Online Retail.xlsx` into `data/raw/` when the file is missing.
